# Public Page Image Audit

The durable inventory, classification, route hierarchy, alt text, disclosures,
delivery rules, and future-proof boundary now live in
[`PREMIUM_MEDIA_SYSTEM.md`](./PREMIUM_MEDIA_SYSTEM.md).

## Current audit result

- The homepage uses three distinct service illustrations instead of one repeated
  residential artwork across every service card.
- `/services` and each service detail page use surface-relevant illustrations
  registered as `not-proof`.
- `/service-areas` no longer uses six civic buildings as primary selection-card
  media.
- Each city page leads with a surface-relevant service illustration and no
  longer renders civic building photography in public content.
- `/request-quote` and `/thank-you` intentionally contain no content imagery.
- No owner-provided completed-project photography is active in the public UI;
  verified project proof remains deferred to issue #51.

The invalid historical service-result WebP references remain excluded. They
must not return to public use without valid browser-renderable assets and the
verification records required by the premium media system.
